#include <kipr/botball.h>

int main()
{
  int move (int, speed_left int, speed_right int, duration){  
    
    
    printf("");
    motor(0,move);
    motor(3,move);
    msleep (duratino);                                   }
    return 0;
}
